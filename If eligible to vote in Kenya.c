//a program to determine if a person is eligible to vote in Kenya
#include<stdio.h>
int main()
{
float age;
float nationality; 
printf("Enter the age");
printf("Enter the nationality");

printf("the person can vote");
return 0;
}